name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: python -m pip install -e ".[test]"
      - name: Tests
        run: python -m pytest -q
      - name: Browser simulation tests
        run: node tests/web_sim_test.mjs
      - name: Browser app syntax
        run: node --check web/app.mjs
      - name: Frozen v0 receipt
        run: python -m experiments.run_v0 > /tmp/newmachine-v0.json
      - name: Frozen v1 receipt
        run: python -m experiments.run_v1 > /tmp/newmachine-v1.json
      - name: Frozen v2 receipt
        run: python -m experiments.run_v2 > /tmp/newmachine-v2.json
      - name: Frozen v3 receipt
        run: python -m experiments.run_v3 > /tmp/newmachine-v3.json
      - name: Frozen v4 receipt
        run: python -m experiments.run_v4 > /tmp/newmachine-v4.json
      - name: Frozen v5 receipt
        run: python -m experiments.run_v5 > /tmp/newmachine-v5.json
